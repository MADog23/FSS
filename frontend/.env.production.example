# Copy the values below into Vercel → Project → Settings → Environment Variables.
# Do NOT commit a filled-in .env.production file to source control.

# Your Railway backend URL — copy from Railway → your backend service → Settings → Domains
# Example: https://financial-safety-backend-production.up.railway.app
VITE_API_URL=https://YOUR_RAILWAY_BACKEND_URL_HERE
